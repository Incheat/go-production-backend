version: "2"
sql:
  - engine: "mysql"
    schema:
      - "migrations/*.up.sql"
    queries: "queries.sql"
    gen:
      go:
        package: "db"
        sql_package: "database/sql"
        out: "../../internal/db/mysql/gen"
        emit_json_tags: true
